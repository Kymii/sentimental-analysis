# sentimental-analysis

**Description**

A web tool that allows users to run natural language processing (NLP) on articles on blogs found on other websites.

**npm build-prod:** This command builds app for production to the dist folder

**npm build-server:** This command starts the server

**npm build-client:** This command runs the app in development and automatically opens localhost:8080 in the browser
